
import theme from 'common/theme';
export const defaultCommonStyles = {
  defaultButtonStyles: {
    color: `${theme.colors.brandWhite}`,
    backgroundColor: `${theme.colors.brandBlue}`,
    padding: 5,
    fontSize: 12,
    width: 50
  }
}

export const defaultLocationEditBtns = {
  backgroundColor: `${theme.colors.brandBlue}`,
  color: `${theme.colors.brandWhite}`,
  paddingLeft: 50,
  paddingRight: 50,
  margin: 0,
  paddingTop: 15,
  paddingBottom: 15,
  maxWidth: 200
}
